<view class="radioBoday">
    <view class="answersBody"  wx:if="{{infos.questionTypeCode == 1}}">
      <view class="content">
        <!-- 选择单选题答案详情 -->
        <view class="oneChoice">
          <view
            class="choseItem li"
            wx:for="("
            :key="index"
          >
          <!-- :class="[
              infos.answerContent != infos.questionAnswer
                ? infos.answerContent == item.questionOption
                  ? 'error'
                  : ''
                : infos.answerContent == item.questionOption
                ? 'right'
                : ''
            ]" -->
            <view
              class="el-p"
            >
            <!-- :class="[
                infos.answerContent == infos.questionAnswer &&
                infos.answerContent == item.questionOption
                  ? 'colors'
                  : ''
              ]" -->
              {{ infos.questionTypeCode }}、
            </view>
            <view
              wx:html="item.questionContent"
              style="width:90%"
              
            ></view>
            <!-- :class="[
                infos.answerContent == infos.questionAnswer &&
                infos.answerContent == item.questionOption
                  ? 'colors'
                  : ''
              ]" -->
            <view
              wx:if="infos.questionAnswer == item.questionOption"
            >
              <image class="right-pic"
                src="../../assets/img/icon_right.png"
                alt=""
                class="right-pic"
              />
            </view>
            <view
            wx:if="1===2"
            >
             <!-- wx:if="
                infos.answerContent == item.questionOption &&
                  infos.answerContent != infos.questionAnswer
              " -->
              <image
                src="../../assets/img/icon_wrong.png"
                alt=""
                class="wrong-pic"
              />
            </view>
          </view>
        </view>
      </view>
    </view>
  </view>
